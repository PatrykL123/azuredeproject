# The main pipeline for spotify_dab
resources:
  pipelines:
    spotify_dab_etl:
      name: spotify_dab_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      
  
      libraries:
        - glob:
            include: ../src/gold/dlt/transformations/**


